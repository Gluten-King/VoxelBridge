/**
 * Minecraft 1.21.4-only adapter extensions.
 *
 * <p>The directory intentionally remains minimal while the 1.21.1 adapter
 * family is source-compatible. Move only the drifting adapter class here when
 * a 1.21.4 API or renderer semantic diverges.</p>
 */
package com.voxelbridge.runtime;
